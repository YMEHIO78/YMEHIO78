"""Skills intelligence pipeline: shared extraction and normalisation logic.

The same functions back the local pandas run (``local/run_analysis.py``) and the
Databricks notebooks, so a rule fixed in one place is fixed everywhere.
"""

__version__ = "1.0.0"
